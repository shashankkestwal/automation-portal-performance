#!/bin/bash -x

# --- Scenario & Locust (Makefile defaults apply for any variable you omit entirely) ---
# export SCENARIO=mvp

# export USERS=10
# export WORKERS=5
# export SPAWN_RATE=2
# export DURATION=10s
# export SCAFFOLDER_TASK_STATUS_DELAY_SECONDS=10
# export LOCUST_EXTRA_CMD="--debug=true"
# export LOCUST_NAMESPACE=locust-operator
#
# --- Namespaces & routes (URLs are built from oc routes when you run `make test`) ---
# export PORTAL_NAMESPACE=self-service-portal
# export AAP_NAMESPACE=ansible-automation-platform
# export PORTAL_ROUTE=sap
# export AAP_ROUTE=aap

# --- Optional Locust tokens (leave empty unless your scenario needs them) ---
# export AAP_ACCESS_TOKEN=
#   # OAuth access token for scaffolder `--aap-access-token` (mvp / ee-builder secrets.aapToken)
# export GITHUB_USER_OAUTH_TOKEN=

# --- core/scale_portal.sh (optional; unset in test.env = no change for that knob) ---
# export PORTAL_REPLICAS=
# export PORTAL_CPU_REQUEST=
# export PORTAL_MEM_REQUEST=
# export PORTAL_CPU_LIMIT=
# export PORTAL_MEM_LIMIT=
# export DB_REPLICAS=
# export DB_CPU_REQUEST=
# export DB_MEM_REQUEST=
# export DB_CPU_LIMIT=
# export DB_MEM_LIMIT=
# export DB_NAME=
# export DB_WORKLOAD_KIND=statefulset
# export DB_MAX_CONNECTIONS=
# export DB_MAX_CONNECTIONS_ENV=POSTGRESQL_MAX_CONNECTIONS
# export DB_MAX_CONNECTIONS_CONTAINER=
